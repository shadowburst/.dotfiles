#!/usr/bin/env bash

output="$(transmission-remote -l)"
total="$(($(echo "$output" | wc -l) - 2))"
downloads="$((total - $(echo "$output" | awk '/100%/' | wc -l)))"
uploads="$((total - downloads))"

echo "{ \"total\": $total, \"downloads\": $downloads, \"uploads\": $uploads }"
